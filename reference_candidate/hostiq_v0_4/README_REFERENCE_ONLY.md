# Reference-only HOSTiQ v0.4 candidate metadata

**REFERENCE_ONLY / NOT DEPLOY AUTHORITY.**

Only non-secret provenance/path/hash metadata is committed here. The 22 selected raw snapshot source files were reviewed locally but intentionally rejected from public commit because the current public-repository secret scanner flags source-level bearer-variable handling. No real secret was identified or copied; raw source remains analytical input only.

`CANDIDATE_PROVENANCE.json` records source archive SHA-256 and per-file path/hash/size/category metadata. It explicitly states that exact live-path bijection is not proven and raw-source public commit is not authorized.
